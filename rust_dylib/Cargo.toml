[package]
name = "rust_dylib"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true  # 必须开启过程宏

[dependencies]
libloading = "0.8"  # 动态库核心依赖
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }
once_cell = "1.19"  # 懒加载静态变量
